ереименуй блок "Вдохнвоение" на советы по работе с поатформой. Прорабоатй более детално и оьширно прям в 3 раза больше странциу кодекса, Ссылку на старицу кодекса также сделай рядом к верним: Мои книги Вдохновение
FAQ на оснвонйо странице. Доавь в инетрфейс елемент стрика тепеа где отображаеться с огоньком сколкьо дней ты пишешь подрд и можн задать целтьв в главазх или словаз а также сделай тдельную страницу привычки писать где будет календарь с упоминанием когда и где ты внес изменнеие, там такде можно бдет доавь коментарии и запларирвоать процесс пистма межул проектами также сделай режим беспотного испольщования ИИ гле нет неоьзодиммости в АПИ (то есть он рабоатет даде елси нет апи в акаеунте) на открытой екосистеме инстуремнтов и мактмально помогает сделать тлже самое что и редими с модялми ии с АПИ где это возможно (как идея для арзитекры вот но сам созадй и возьми улчшие решения: I-ядро (онлайн)

Используем браузерный JS LLM:

Transformers.js

TinyLLaMA / Phi-3 mini / MPT mini

Запуск модели через WebGPU прямо в браузере.

Хранение состояния модели в IndexedDB или localStorage, чтобы привязать работу пользователя к аккаунту Google.

2.3 Система авторизации

OAuth через Google.

После авторизации все действия и сохранённые проекты привязываются к аккаунту.

Возможна синхронизация данных между устройствами через IndexedDB + optional Google Drive интеграцию (для хранения проектов).

2.4 Инструменты редакции текста

Автоабзацы, исправление грамматики, рерайт — реализуются через:

локальные JS алгоритмы + WebLLM (для грамматики можно использовать LanguageTool WASM версию, полностью в браузере)

Функции:

выделить текст → подсказки по форматированию
автоабзацы → разбиение текста на логические блоки
рерайт → улучшение стиля и формулировок). Самое важное чтобы это было без сторнних октыртх редакторов все щашито внутри приложения и на страице с моделями чтобы было отдельное сравнение испотщвоание плоатныз моделей и беспоатный редим с опсианием екосисемы рабоыт бесипного режима

